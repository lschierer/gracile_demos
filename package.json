{
  "name": "@gracile/template--minimal",
  "type": "module",
  "version": "0.0.2",
  "scripts": {
    "dev": "gracile-dev --root .",
    "build": "gracile build --root .",
    "preview": "gracile preview"
  },
  "dependencies": {
    "@gracile/gracile": "0.1.1"
  },
  "devDependencies": {
    "typescript": "5.5.3",
    "vite-tsconfig-paths": "^4.3.2"
  }
}
